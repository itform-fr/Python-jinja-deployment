$TTL	86400
{{domain}}.	    IN	SOA	    {{host}}.{{domain}}. jcohen.itform.fr. (
			                        1		; Serial
			             604800		  ; Refresh
			               86400		; Retry
			           2419200		; Expire
			               86400 )	; Negative Cache TTL
;
                    		IN	NS  {{host}}.{{domain}}.
{{host}}		               A    {{ip}}
{{alias}}		     CNAME  {{host}}
